require 'spec_helper'

describe Sidekiq::Hierarchy do
  it 'has a version number' do
    expect(Sidekiq::Hierarchy::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
