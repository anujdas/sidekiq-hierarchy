source 'https://rubygems.org'

group :test do
  # for server middleware test harness; unnecessary for normal use,
  # or when change gets released as a final version
  gem 'sidekiq', git: 'https://github.com/mperham/sidekiq.git'

  gem 'pry'
end

gemspec
